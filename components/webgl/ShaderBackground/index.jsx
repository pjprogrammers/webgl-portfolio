"use client";

/**
 * @deprecated Usar `GlobalParticleCanvas` — incluye shader + partículas en un solo contexto WebGL.
 * Se mantiene como alias para compatibilidad.
 */
export {
  default,
  DEFAULT_SHADER_COLORS,
  DEFAULT_COLOR_WEIGHTS,
  DEFAULT_BLOB_RANDOMNESS,
  DEFAULT_BLOB_DISPLACEMENT,
  DEFAULT_BLOB_MORPH_SPEED,
  DEFAULT_SCROLL_GRADIENT_DOWN_SPEED,
  DEFAULT_SCROLL_GRADIENT_UP_SPEED,
  DEFAULT_COLOR_BLEND,
  CSS_FALLBACK_GRADIENT,
} from "../FooterRCanvas";

export {
  DEFAULT_BLOOM_INTENSITY,
  DEFAULT_BLOOM_THRESHOLD,
  DEFAULT_BLOOM_SMOOTHING,
} from "../FooterRCanvas/shaderBackgroundConfig.js";
