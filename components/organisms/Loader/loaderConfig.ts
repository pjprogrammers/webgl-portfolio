/** Tiempo visible antes de la animación de salida del loader inicial. */
export const LOADER_HOLD_DURATION = 2;

/** Duración del split vertical (leftToBottom / rightToTop). */
export const LOADER_SPLIT_DURATION = 1.9;

/** Retraso de rightToTop respecto al inicio de leftToBottom. */
export const LOADER_SPLIT_DELAY = 0.1;
